  <% if (typeof info !== 'undefined' && info.length > 0) {%>
    <div class="row mt-5">
      <div class="col-md-10 m-auto">
        <div class="card card-body">
          <div class="text-center"> 
          <p class="h4 mb-4">Counsult</p>
          </div>
          <form action="/DocRecEntry" method="POST">
          <%for(var j=0; j< info[0].length;j++){ %>
            <!-- First name -->
            <label><i class="fas fa-user"></i> Patient ID </label>
            <input type="text" id="id" class="form-control mb-4" placeholder="<%= info[0][j].p_id %>" name="p_id" disabled/>

            <!-- First name -->
            <label><i class="fas fa-user"></i> Name </label>
            <input type="text" id="name" class="form-control mb-4" placeholder="<%= info[0][j].p_name %>" name="name" disabled/>

            <!-- Age -->
            <label><i class="fas fa-user"></i> Age </label>
            <input type="text" id="age" class="form-control mb-4" placeholder="<%= info[0][j].p_age %>" name="age" disabled/>
            
            <!-- Age -->
            <label><i class="fas fa-user"></i> Gender </label>
            <input type="text" id="gender" class="form-control mb-4" placeholder="<%= info[0][j].p_gender %>" name="gender" disabled/>
            
            <!-- Age -->
            <label><i class="fas fa-user"></i> BloodGroup </label>
            <input type="text" id="BloodGroup" class="form-control mb-4" placeholder="<%= info[0][j].p_bloodgrp %>" name="BloodGroup" disabled/>
          <%}%>
            <div class="form-group">
              <label for="exampleFormControlTextarea2">Description</label>
              <textarea class="form-control rounded-0" id="exampleFormControlTextarea2" name="desc" rows="3"></textarea>
            </div>
            <div class="row centered-form">
              <div class="col-xs-12 col-sm-8 col-md-8 col-sm-offset-2 col-md-offset-2">
                  <div class="panel panel-info">
                      <div class="panel-body">
                          <div class="list_wrapper">  
                              <div class="row">
                                  <div class="col-xs-7 col-sm-7 col-md-7">
                                      <div class="form-group">
                                        <label><i class="fas fa-user-md"></i> Med Name </label>
                                        <select class="browser-default custom-select col-md-14" name="med_name">
                                          <option value= "NA"> NA </option>
                                          <% if (typeof info !== 'undefined' && info.length > 0) {%>
                                            <%for (var i = 0; i < info[1].length; i++) { %>
                                              <option value="<%= info[1][i].m_name %>"> <%= info[1][i].m_name %> </option>
                                            <%}%>
                                          <% } %>
                                        </select>
                                      </div>
                                  </div>
                                  <div class="col-xs-7 col-sm-7 col-md-7">
                                      <div class="form-group">
                                        <label><i class="fas fa-capsules"></i>  morning-afternoon-dinner </label>
                                          <input autocomplete="off" name="mad" type="text" placeholder="Type Item Quantity" class="form-control"/>
                                      </div>
                                  </div>
                                  <div class="col-xs-7 col-sm-7 col-md-7">
                                    <div class="form-group">
                                      <label><i class="fas fa-capsules"></i>  Quantity </label>
                                        <input autocomplete="off" name="quantity" type="number" placeholder="Quantity" class="form-control"/>
                                    </div>
                                  </div> 
                                  <div class="col-xs-1 col-sm-1 col-md-1">
                                      <button class="btn btn-primary list_add_button" type="button">+</button>
                                  </div>
                              </div>
                          </div>
                      </div>
                  </div>
              </div>
          </div>
          <div class="row centered-form">
            <div class="col-xs-12 col-sm-8 col-md-8 col-sm-offset-2 col-md-offset-2">
                <div class="panel panel-info">
                    <div class="panel-body">
                        <div class="list_wrapper2">  
                            <div class="row2">
                                <div class="col-xs-7 col-sm-7 col-md-7">
                                    <div class="form-group">
                                      <label><i class="fas fa-briefcase-medical"></i> Lab Test  </label>
                                      <select class="browser-default custom-select col-md-14" name="lab_name">
                                        <option value= "NA"> NA </option>
                                        <% if (typeof info !== 'undefined' && info.length > 0) {%>
                                          <%for (var i = 0; i < info[1].length; i++) { %>
                                            <option value="<%= info[1][i].l_name %>"> <%= info[1][i].l_name %> </option>
                                          <%}%>
                                        <% } %>
                                      </select>
                                    </div>
                                </div>
                                <div class="col-xs-1 col-sm-1 col-md-1">
                                    <button class="btn btn-primary list_add_button2" type="button">+</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <button type="submit" class="btn blue-gradient">Blue</button>
        </form>
</div>
</div>
</div>

<%}%>


<% if (typeof OutApp !== 'undefined' && OutApp.length > 0) {%>
<%for (var i = 0; i < OutApp.length; i++) { %>
  <form action="/OutApp" method="POST">
  <!-- First name -->
    <label><i class="fas fa-user"></i> Patient ID </label>
    <input type="text" id="id" class="form-control mb-4" placeholder="<%= OutApp[i].p_id %>" name="p_id" disabled/>

    <!-- First name -->
    <label><i class="fas fa-user"></i> Name </label>
    <input type="text" id="name" class="form-control mb-4" placeholder="<%= OutApp[i].p_name %>" name="name" disabled/>

    <!-- Age -->
    <label><i class="fas fa-user"></i> Age </label>
    <input type="text" id="age" class="form-control mb-4" placeholder="<%= OutApp[i].p_age %>" name="age" disabled/>
    
    <!-- Age -->
    <label><i class="fas fa-user"></i> Gender </label>
    <input type="text" id="gender" class="form-control mb-4" placeholder="<%= OutApp[i].p_gender %>" name="gender" disabled/>
    
    <!-- Age -->
    <label><i class="fas fa-user"></i> BloodGroup </label>
    
    <input type="text" id="BloodGroup" class="form-control mb-4" placeholder="<%= OutApp[i].p_bloodgrp %>" name="BloodGroup" disabled/>
    
    <!-- DOA -->
    <div class="dates">
    <label><i class="fas fa-calendar"></i> Date Appointment </label><br>
    <input type="text" autocomplete="off" id="user1" class="form-control" placeholder="Date of Appointment" name="date" required><br>
    </div>
    
    <label><i class="fas fa-user-md"></i> Position </label><br>
      <select class="browser-default custom-select mb-4" name="spe">
        <% if (typeof diseases !== 'undefined' && diseases.length > 0) {%>
          <%for (var i = 0; i < diseases.length; i++) { %>
            <option value="<%= diseases[i].d_id %>"> <%= diseases[i].d_name %> </option>
          <%}%>
        <% } %>
      </select>
      
      <label><i class="fas fa-hand-holding-usd"></i> Consultancy Fee </label><br>
      <select class="browser-default custom-select mb-4" name="consultancy_fee">
          <% if (typeof diseases !== 'undefined' && diseases.length > 0) {%>
            <%for (var i = 0; i < diseases.length; i++) { %>
              <option value="<%= diseases[i].consultancy_fee %>"> <%= diseases[i].d_name %> : <%= diseases[i].consultancy_fee %> </option>
            <%}%>
          <% } %>
      </select>
      <br>
    <button type="submit" class="btn blue-gradient">Blue</button>
  </form>
<%}%>
<%}%>


<% if (typeof queue !== 'undefined' && queue.length > 0) {%>  
  <form method="POST">  
      <table class="table">
        <thead class="black white-text">
          <tr>
            <th scope="col">Patient ID</th>
            <th scope="col">Patient Name</th>
            <th scope="col">Patient age</th>
            <th scope="col">Patient Status</th>
          </tr>
        </thead>
        <tbody>
          <%for (var i = 0; i < queue[0].length; i++) { %>
            <tr>
            <th scope="row"><%= queue[0][i].p_id %></th>
            <td><%= queue[1][i].p_name %></td>
            <td><%= queue[1][i].p_age %></td>
            <% if(queue[0][i].prescription == 'NA'){ %>
              <td><button type="submit" class="btn btn-outline-danger waves-effect" formaction="/PatientID" name="PatientID" value="<%= queue[0][i].opd_id %>">In_Active</button></td>
            <%}else{%>
              <td>Active</td>
            <%}%>
          </tr>
          <%}%>
          
        </tbody>
      </table>
      </form>
  <%}%>
  

<% if (typeof prescription !== 'undefined' && prescription.length > 0) {%>  
  <form action="/PharmacyPayment" method="POST">
  <div class="row mt-5">
    <div class="col-md-10 m-auto">
      <div class="card card-body">
        <div class="text-center"> 
        <p class="h4 mb-4">Patient's Info</p>
        </div>
          <!-- First name -->
          <label><i class="fas fa-user"></i> Patient Name </label>
          <input type="text" id="name" class="form-control mb-4" placeholder="<%= prescription[0].doa %>" name="DOA" disabled/>

          <div class="form-group">
            <label for="exampleFormControlTextarea2"><i class="fas fa-comment-medical"></i> Doctor Prescription</label>
            <textarea class="form-control rounded-0" id="exampleFormControlTextarea2" rows="3" disabled><%= prescription[0].meds %></textarea>
          </div>
          <div class="row centered-form">
            <div class="col-xs-12 col-sm-8 col-md-8 col-sm-offset-2 col-md-offset-2">
                <div class="panel panel-info">
                    <div class="panel-body">
                        <div class="list_wrapper01">  
                            <div class="row01">
                                <div class="col-xs-7 col-sm-7 col-md-7">
                                    <div class="form-group">
                                      <label><i class="fas fa-user-md"></i> Med Name </label>
                                      <select class="browser-default custom-select col-md-14" name="med_name">
                                        <option value= "NA"> NA </option>
                                         <%for (var i = 0; i < prescription.length; i++) { %>
                                            <option value="<%= prescription[i].m_name %>"> Med_name: <%=prescription[i].m_name%>, Med_quantity : <%=prescription[i].m_quantity%>, Med_price : <%=prescription[i].m_price%> </option>
                                          <%}%>
                                      </select>
                                    </div>
                                </div>
                                <div class="col-xs-7 col-sm-7 col-md-7">
                                    <div class="form-group">
                                      <label><i class="fas fa-capsules"></i>  Quantity </label>
                                        <input autocomplete="off" name="quantity" type="text" placeholder="Quantity" class="form-control"/>
                                    </div>
                                </div>
                                <div class="col-xs-7 col-sm-7 col-md-7">
                                  <div class="form-group">
                                    <label><i class="fas fa-capsules"></i> Price </label>
                                      <input autocomplete="off" name="price" type="text" placeholder="Price" class="form-control"/>
                                  </div>
                                </div> 
                                <div class="col-xs-1 col-sm-1 col-md-1">
                                    <button class="btn btn-primary list_add_button01" type="button">+</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <button type="submit" class="btn blue-gradient">Payment</button>
      </div>
    </div>
  </div>
</form>
<%}%>

<% if (typeof pl !== 'undefined' && pl.length > 0) {%>
  <form action="/labpay" method="POST">  
  <div class="row mt-5">
    <div class="col-md-10 m-auto">
      <div class="card card-body">
        <div class="text-center"> 
        <p class="h4 mb-4">Lab Fee</p>
        </div>
        <!-- Default checked -->
        <% for(var i=0; i< pl.length; i++) { %>
          <input name="fee" value="<%= pl[i][0].l_name %>: Rs. <%= pl[i][0].l_cost %>" type="checkbox" onclick="totalIt()" /> <%= pl[i][0].l_name %>: Rs. <%= pl[i][0].l_cost %>
          <br>
          <% } %>
          <div class="md-form input-with-post-icon">
            <i class="fas fa-user input-prefix"></i>
            <input type="text" id="suffixInside" class="form-control" value="Rs. 0.00"  readonly="readonly" name="total">
            <label for="suffixInside">Total Bill </label>
        </div>
        <button type="submit" class="btn blue-gradient">Payment</button>
      </div>
    </div>
  </div>
</form>
<%}%>
    
